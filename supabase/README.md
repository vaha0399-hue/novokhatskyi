# Supabase

This directory is reserved for future Supabase/PostgreSQL project assets.

No database schema is defined during Stage 1. Schema design may begin only
after real API-Football responses have been collected and analysed.

All future database changes must be represented by reviewed migrations in
`supabase/migrations/`; production data must not be changed manually as part of
normal development.
